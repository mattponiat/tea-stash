import { extendTheme } from "@chakra-ui/react";

export const customTheme = extendTheme({
  fonts: {
    heading: "nunito",
    body: "nunito",
  },
  colors: {
    mainBeige: "#FFF7E7",
    secondaryGreen: "#749B74",
  },
});
